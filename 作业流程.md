# 作业流程（先拉老师更新，再做作业，再推送自己仓库）

这份清单按你当前仓库结构设计：
- 老师仓库：`upstream`
- 你的仓库：`origin`
- 默认主分支：`main`

---

## 0. 一次性初始化（首次配置时执行）

```bash
git remote -v
# 作用：查看远程仓库是否已经配置好 origin / upstream

git remote add upstream git@github.com:baojian/llm-26.git
# 作用：添加老师仓库为 upstream（如果已存在会报错，可忽略）

git fetch upstream
# 作用：从老师仓库抓取最新提交到本地，不改动你的工作文件
```

---

## 1. 每次开始作业前（先同步老师最新）

```bash
git checkout main
# 作用：切回主分支，避免在错误分支上同步

git fetch upstream
# 作用：抓取老师仓库最新提交

git pull --rebase upstream main
# 作用：把老师 main 线性合并到你本地 main，减少无意义 merge commit

git push origin main
# 作用：把已同步后的 main 推到你自己的远程仓库，保证 origin/main 也最新
```

---

## 2. 开始写当前作业（推荐新建作业分支）

```bash
git checkout -b hw-01
# 作用：从当前 main 新建作业分支，隔离本次作业改动
```

写代码/做 notebook 后提交：

```bash
git status
# 作用：查看哪些文件被修改、是否准备提交

git add .
# 作用：暂存本次全部改动（若想精细控制可改成 git add <文件>）

git commit -m "完成 hw-01"
# 作用：生成一次可追踪提交
```

---

## 3. 推送到你的仓库

```bash
git push -u origin hw-01
# 作用：首次把作业分支推到你的远程仓库，并建立上游跟踪关系
```

后续同一分支继续提交时：

```bash
git push
# 作用：因为已建立跟踪关系，可直接推送当前分支
```

---

## 4. 若你习惯直接交在 main（不推荐，但可用）

```bash
git checkout main
# 作用：切回主分支开发

git add .
# 作用：暂存改动

git commit -m "完成 hw-01"
# 作用：提交作业

git push origin main
# 作用：推送 main 到你的远程仓库
```

---

## 5. 常见问题处理

### 5.1 拉老师更新前，本地有未提交改动

```bash
git stash
# 作用：临时收起当前未提交改动

git pull --rebase upstream main
# 作用：先把老师最新同步到本地 main

git stash pop
# 作用：恢复刚才临时收起的改动
```

### 5.2 看提交历史是否正确

```bash
git log --oneline --graph --decorate -20
# 作用：快速查看最近 20 条提交与分支指向
```

### 5.3 不确定当前在哪个分支

```bash
git branch -vv
# 作用：查看当前分支、跟踪分支及最新提交
```

---

## 6. 每周最短流程（可直接照抄）

```bash
git checkout main
git fetch upstream
git pull --rebase upstream main
git push origin main
git checkout -b hw-XX
# 做作业...
git add .
git commit -m "完成 hw-XX"
git push -u origin hw-XX
```

这套流程核心原则：
- `upstream` 负责“拉老师更新”
- `origin` 负责“推你自己的作业”
- 开始前先同步，完成后再推送
